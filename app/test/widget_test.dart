import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without error', (WidgetTester tester) async {
    // Placeholder — real tests will be added per task
    expect(true, isTrue);
  });
}
